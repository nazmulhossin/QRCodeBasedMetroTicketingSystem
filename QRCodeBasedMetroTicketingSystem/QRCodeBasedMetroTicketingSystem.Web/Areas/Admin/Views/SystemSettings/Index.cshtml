@model SettingsViewModel

@{
    ViewData["Title"] = "System Settings";
}

<div class="container-fluid">
    <!-- Page Header -->
    <div class="row mb-2">
        <div class="col-12 page-header">
            <h1 class="page-title">System Settings</h1>
            <div class="breadcrumb">
                <span><a asp-area="Admin" asp-controller="Dashboard" asp-action="Index">Home</a></span><i class="bi bi-chevron-right"></i><span>System Settings</span>
            </div>
        </div>
    </div>

    <div class="row">
        <div class="col-12">
            <div class="card shadow-sm p-3">
                <div class="card-body">
                    <form asp-area="Admin" asp-controller="SystemSettings" asp-action="Index" method="POST">
                        <div class="row mb-3">
                            <div class="col-md-6 pe-3">
                                <label asp-for="MinFare" class="form-label">Minimum Fare (TK)</label>
                                <input asp-for="MinFare" class="form-control" step="0.000001" min="0" disabled required />
                            </div>

                            <div class="col-md-6 ps-3">
                                <label asp-for="FarePerKm" class="form-label">Fare Per Kilometer (TK)</label>
                                <input asp-for="FarePerKm" class="form-control" step="0.000001" min="0" disabled required />
                            </div>
                        </div>

                        <div class="row mb-3">
                            <div class="col-md-6 pe-3">
                                <label asp-for="RapidPassQrCodeValidityMinutes" class="form-label">Rapid Pass QR Code Validity (minutes)</label>
                                <input asp-for="RapidPassQrCodeValidityMinutes" class="form-control" step="1" min="1" disabled required />
                            </div>

                            <div class="col-md-6 ps-3">
                                <label asp-for="QrTicketValidityMinutes">QR Ticket Validity (minutes)</label>
                                <input asp-for="QrTicketValidityMinutes" class="form-control" step="1" min="1" disabled required />
                            </div>
                        </div>

                        <div class="row mb-3">
                            <div class="col-md-6 pe-3">
                                <label asp-for="MaxTripDurationMinutes" class="form-label">Maximum Trip Duration (minutes)</label>
                                <input asp-for="MaxTripDurationMinutes" class="form-control" step="1" min="1" disabled required />
                            </div>
                            <div class="col-md-6 ps-3">
                                <label asp-for="TimeLimitPenaltyFee" class="form-label">Time Limit Penalty Fee (Tk)</label>
                                <input asp-for="TimeLimitPenaltyFee" class="form-control" step="1" min="0" disabled required />
                            </div>
                        </div>
                        
                        <div class="d-flex justify-content-end gap-2">
                            <a asp-area="Admin" asp-controller="SystemSettings" asp-action="Edit" class="btn btn-success">
                                <i class="bi bi-pencil-square me-1"></i> Edit Settings
                            </a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
